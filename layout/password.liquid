<!doctype html>
<html lang="{{ request.locale.iso_code }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>{{ shop.name }}</title>
  {{ content_for_header }}
  {{ 'glazur-fonts.css' | asset_url | stylesheet_tag }}
  {{ 'glazur-tokens.css' | asset_url | stylesheet_tag }}
  {{ 'glazur-base.css' | asset_url | stylesheet_tag }}
  {{ 'glazur-components.css' | asset_url | stylesheet_tag }}
</head>
<body class="password" style="min-height: 100vh; display: flex; align-items: center; justify-content: center; background: var(--color-bg, #fdf8fa);">
  {{ content_for_layout }}
</body>
</html>
